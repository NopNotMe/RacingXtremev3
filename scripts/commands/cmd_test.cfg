// list commands
cmd_gravity = [
	if (=s $arg2 1) [
		gamespeed 70
		say (unescape $racemap) // test if has access to $racemap
	] [
		gamespeed 100
	]
]

registercommand "gravity" cmd_gravity 3 "|s" "^gravity (command) ^f1Slows gamemode to give anit-gravity feel."
